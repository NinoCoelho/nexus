/**
 * UnifiedGraphView — thin shim for backward compatibility with App.tsx.
 *
 * The actual implementation lives in UnifiedGraph/. This file exists so
 * App.tsx's import path doesn't need to change.
 */

export { default } from "./UnifiedGraph";
